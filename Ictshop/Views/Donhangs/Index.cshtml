@model IEnumerable<Ictshop.Models.Donhang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 style=" padding: 30px; color: #f542c2; font-weight: bold;">DANH SÁCH ĐƠN HÀNG </h2>


<table class="table">
    <tr>
        <th>
           Họ tên
        </th>
        <th>
            Ngày đặt
        </th>
        <th>
            Tình trạng đơn hàng
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nguoidung.Hoten)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ngaydat)
        </td>
        <td>
            <div style="color:red">Đang chờ xác nhận</div>
        </td>
        <td>
            <button id="bgnGioHang">
                @Html.ActionLink("Chi tiết", "Details", new { id = item.Madon }, new { @style = "color:white;" })
            </button>
            <button id="bgnGioHang">
                @Html.ActionLink("Hủy Đơn", "Delete", new { id = item.Madon }, new { @style = "color:white;" })
            </button>
        </td>
    </tr>
}

</table>
